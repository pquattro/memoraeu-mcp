configSchema:
  type: object
  required:
    - apiKey
  properties:
    apiKey:
      type: string
      title: "MemoraEU API Key"
      description: "Your API key from app.memoraeu.com → Settings → API Keys"
      x-from:
        header: "x-memoraeu-api-key"
      x-to:
        query: "token"
